aside#adminSidebar.sider-admin.d-flex.flex-column(
  style="min-height: 100vh; width: 260px; transition: all 0.3s ease;"
)
  nav.nav.flex-column.py-4.px-3
    // Sidebar header
    h5.mb-4.d-flex.align-items-center.px-3.text-uppercase.text-primary.font-weight-bold
      i.fas.fa-cogs.mr-2
      | Quản trị

    // Navigation links
    a.nav-link.d-flex.align-items-center.rounded.mb-1.px-3.py-2(
      href=prefixAdmin + '/dashboard',
      class=(currentPath.startsWith(prefixAdmin + '/dashboard') ? 'active' : '')
    )
      i.fas.fa-home.mr-3
      | Tổng quan

    a.nav-link.d-flex.align-items-center.rounded.mb-1.px-3.py-2(
      href=prefixAdmin + '/products',
      class=(currentPath.startsWith(prefixAdmin + '/products') ? 'active' : '')
    )
      i.fas.fa-box.mr-3
      | Sản phẩm

    a.nav-link.d-flex.align-items-center.rounded.mb-1.px-3.py-2(
      href=prefixAdmin + '/blogs',
      class=(currentPath.startsWith(prefixAdmin + '/blogs') ? 'active' : '')
    )
      i.fas.fa-newspaper.mr-3
      | Bài viết